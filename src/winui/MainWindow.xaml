<Window
    x:Class="PS2DriveForge.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PS2DriveForge.WinUI"
    Title="PS2 DriveForge — Emilia">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBarDragRegion" Grid.Row="0" Padding="16,0,148,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <Border Width="28" Height="28" Background="{StaticResource DriveForgeAccentBrush}" CornerRadius="7">
                    <FontIcon Glyph="&#xE7F8;" FontSize="15" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <TextBlock Text="PS2 DriveForge" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Border x:Name="VersionBadge" Background="{StaticResource DriveForgeAccentSubtleBrush}" CornerRadius="6" Padding="7,2">
                        <TextBlock Text="Emilia 0.5" FontSize="10" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>
            </StackPanel>
            <Border x:Name="ReadOnlyBadge" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Background="{StaticResource DriveForgeCautionBackgroundBrush}" CornerRadius="12" Padding="9,3">
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <FontIcon Glyph="&#xE72E;" FontSize="10" Foreground="{StaticResource DriveForgeCautionBrush}"/>
                    <TextBlock Text="READ ONLY" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource DriveForgeCautionBrush}"/>
                </StackPanel>
            </Border>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                <Button x:Name="OpenImageButton" Style="{StaticResource DriveForgeIconButtonStyle}" ToolTipService.ToolTip="Open disk image">
                    <FontIcon Glyph="&#xE8E5;" FontSize="15"/>
                </Button>
                <Button x:Name="RescanButton" Style="{StaticResource DriveForgeIconButtonStyle}" ToolTipService.ToolTip="Rescan PS2 drives">
                    <FontIcon Glyph="&#xE72C;" FontSize="15"/>
                </Button>
            </StackPanel>
        </Grid>

        <NavigationView x:Name="Navigation" Grid.Row="1"
                        Background="Transparent"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="True"
                        PaneDisplayMode="Auto"
                        IsPaneOpen="True"
                        OpenPaneLength="190"
                        CompactPaneLength="48"
                        IsPaneToggleButtonVisible="True">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="HDD Manager" Tag="drive" IsSelected="True">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE7F8;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Files" Tag="files">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE8B7;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Mount" Tag="mount">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xEDA2;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Performance" Tag="performance">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE9D9;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <InfoBar x:Name="AccessInfoBar" Grid.Row="0" IsOpen="False" Severity="Warning"
                         Title="Raw disk access needs Administrator privileges"
                         Message="DriveForge can still open disk images. Restart elevated to scan physical PS2 HDDs."
                         Margin="18,12,18,10">
                    <InfoBar.ActionButton>
                        <Button x:Name="RestartAdminInfoButton" Content="Restart as Administrator"/>
                    </InfoBar.ActionButton>
                </InfoBar>

                <ScrollViewer x:Name="HddPage" Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              HorizontalScrollMode="Disabled"
                              HorizontalContentAlignment="Stretch">
                    <Grid x:Name="HddContent" Padding="24,20,24,28" MaxWidth="1560" HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="4">
                                <TextBlock Text="HDD Manager" Style="{ThemeResource TitleTextBlockStyle}"/>
                                <TextBlock Text="APA appears first; HDL metadata is enriched in the background."
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel x:Name="HddHeaderActions" Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="0,10,0,0">
                                <Button x:Name="MountButton" Content="Mount read-only" Style="{StaticResource DriveForgeToolbarButtonStyle}" IsEnabled="False"/>
                                <Button x:Name="OpenExplorerButton" Content="Open in Explorer" Style="{StaticResource DriveForgeToolbarButtonStyle}" IsEnabled="False"/>
                            </StackPanel>
                        </Grid>

                        <Border Grid.Row="1" Style="{StaticResource DriveForgeCardStyle}" Margin="0,0,0,12">
                            <Grid ColumnSpacing="14" RowSpacing="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="42" Height="42" CornerRadius="10" Background="{StaticResource DriveForgeAccentSubtleBrush}" VerticalAlignment="Center">
                                    <FontIcon Glyph="&#xEDA2;" FontSize="19" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Grid.Column="1" Spacing="3" VerticalAlignment="Center">
                                    <TextBlock Text="Source" Style="{StaticResource DriveForgeMetricLabelStyle}"/>
                                    <TextBlock x:Name="SourceNameText" Text="No PS2 HDD opened" FontSize="17" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock x:Name="SourceDetailText" Text="Scanning starts automatically" Style="{StaticResource DriveForgeCaptionStyle}" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <Border Grid.Column="2" CornerRadius="10" Padding="8,3" VerticalAlignment="Center" Background="{ThemeResource ControlFillColorSecondaryBrush}">
                                    <TextBlock x:Name="SourceMediaText" Text="waiting" Style="{StaticResource DriveForgeCaptionStyle}"/>
                                </Border>
                                <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Spacing="8">
                                    <ComboBox x:Name="DriveSelector" MinWidth="200" PlaceholderText="Detected PS2 HDDs" HorizontalAlignment="Stretch"/>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button x:Name="OpenDriveButton" Content="Open selected" Style="{StaticResource DriveForgeToolbarButtonStyle}" IsEnabled="False"/>
                                        <Button x:Name="OpenImageInlineButton" Content="Open image" Style="{StaticResource DriveForgeToolbarButtonStyle}"/>
                                        <Button x:Name="ScanDrivesButton" Content="Scan drives" Style="{StaticResource DriveForgeToolbarButtonStyle}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <ItemsControl Grid.Row="2" Margin="0,0,0,12">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Orientation="Horizontal" ItemWidth="210" ItemHeight="92"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <Border Style="{StaticResource DriveForgeCardStyle}" Margin="0,0,10,10">
                                <StackPanel Spacing="4"><TextBlock Text="APA rows" Style="{StaticResource DriveForgeMetricLabelStyle}"/><TextBlock x:Name="ApaRowsText" Text="—" Style="{StaticResource DriveForgeMetricValueStyle}"/><TextBlock Text="zero-I/O catalog" Style="{StaticResource DriveForgeCaptionStyle}"/></StackPanel>
                            </Border>
                            <Border Style="{StaticResource DriveForgeCardStyle}" Margin="0,0,10,10">
                                <StackPanel Spacing="4"><TextBlock Text="HDL games" Style="{StaticResource DriveForgeMetricLabelStyle}"/><TextBlock x:Name="HdlGamesText" Text="—" Style="{StaticResource DriveForgeMetricValueStyle}"/><TextBlock x:Name="EnrichmentStateText" Text="metadata idle" Style="{StaticResource DriveForgeCaptionStyle}"/></StackPanel>
                            </Border>
                            <Border Style="{StaticResource DriveForgeCardStyle}" Margin="0,0,10,10">
                                <StackPanel Spacing="4"><TextBlock Text="Backing I/O" Style="{StaticResource DriveForgeMetricLabelStyle}"/><TextBlock x:Name="BackingReadsText" Text="—" Style="{StaticResource DriveForgeMetricValueStyle}"/><TextBlock Text="reads this session" Style="{StaticResource DriveForgeCaptionStyle}"/></StackPanel>
                            </Border>
                            <Border Style="{StaticResource DriveForgeCardStyle}" Margin="0,0,10,10">
                                <StackPanel Spacing="4"><TextBlock Text="Source size" Style="{StaticResource DriveForgeMetricLabelStyle}"/><TextBlock x:Name="SourceSizeText" Text="—" Style="{StaticResource DriveForgeMetricValueStyle}"/><TextBlock Text="reported capacity" Style="{StaticResource DriveForgeCaptionStyle}"/></StackPanel>
                            </Border>
                        </ItemsControl>

                        <Border Grid.Row="3" Style="{StaticResource DriveForgeCompactCardStyle}" Margin="0,0,0,12">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <Border Width="32" Height="32" CornerRadius="8" Background="{StaticResource DriveForgeAccentSubtleBrush}"><FontIcon Glyph="&#xE895;" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                <StackPanel Grid.Column="1" Spacing="5" VerticalAlignment="Center">
                                    <Grid><TextBlock Text="Game metadata enrichment" Style="{StaticResource DriveForgeSectionTitleStyle}"/><TextBlock x:Name="EnrichmentProgressText" Text="Waiting for a source" HorizontalAlignment="Right" Style="{StaticResource DriveForgeCaptionStyle}"/></Grid>
                                    <ProgressBar x:Name="EnrichmentProgress" Minimum="0" Maximum="1" Value="0" Height="3"/>
                                </StackPanel>
                                <Button Grid.Column="2" x:Name="CancelEnrichmentButton" Content="Cancel" IsEnabled="False" Style="{StaticResource DriveForgeToolbarButtonStyle}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <Grid x:Name="HddWorkspace" Grid.Row="4" RowSpacing="12">
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <Border Grid.Row="0" Style="{StaticResource DriveForgeCardStyle}" Padding="0" MinHeight="390">
                                <Grid>
                                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="40"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                    <Grid Padding="14,12" ColumnSpacing="8" RowSpacing="8">
                                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBox x:Name="FilterBox" Grid.ColumnSpan="3" PlaceholderText="Search title, startup ID or partition…" MinWidth="120"/>
                                        <ComboBox x:Name="PartitionKindFilter" Grid.Row="1" SelectedIndex="0" MinWidth="126">
                                            <ComboBoxItem Content="All partitions"/><ComboBoxItem Content="HDL games"/><ComboBoxItem Content="PFS"/><ComboBoxItem Content="System"/>
                                        </ComboBox>
                                        <ToggleSwitch x:Name="ShowSubsToggle" Grid.Row="1" Grid.Column="1" Header="Sub-partitions" IsOn="True" VerticalAlignment="Center"/>
                                        <Button x:Name="RefreshMetadataButton" Grid.Row="1" Grid.Column="2" Style="{StaticResource DriveForgeIconButtonStyle}" ToolTipService.ToolTip="Refresh metadata"><FontIcon Glyph="&#xE72C;" FontSize="14"/></Button>
                                    </Grid>
                                    <Grid Grid.Row="1" Padding="14,0" Background="{ThemeResource SubtleFillColorSecondaryBrush}" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="2.4*"/><ColumnDefinition Width="1.05*"/><ColumnDefinition Width="1*"/><ColumnDefinition Width="0.9*"/><ColumnDefinition Width="0.85*"/><ColumnDefinition Width="0.8*"/></Grid.ColumnDefinitions>
                                        <TextBlock Text="Title / partition" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/><TextBlock Grid.Column="1" Text="Startup" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/><TextBlock Grid.Column="2" Text="Type" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/><TextBlock Grid.Column="3" Text="Allocated" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/><TextBlock Grid.Column="4" Text="LBA" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/><TextBlock Grid.Column="5" Text="State" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/>
                                    </Grid>
                                    <Grid Grid.Row="2" MinHeight="300">
                                        <ListView x:Name="PartitionList" SelectionMode="Single" HorizontalContentAlignment="Stretch" Background="Transparent"/>
                                        <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10" Margin="20" IsHitTestVisible="False" MaxWidth="420">
                                            <Border Width="58" Height="58" CornerRadius="16" Background="{StaticResource DriveForgeAccentSubtleBrush}"><FontIcon Glyph="&#xE7F8;" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                            <TextBlock Text="Open a PS2 HDD or disk image" FontSize="17" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                            <TextBlock Text="DriveForge will publish the APA catalog, then enrich HDL metadata without blocking the UI." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextAlignment="Center" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                            <ItemsControl Grid.Row="1" Margin="0,0,0,4">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><ItemsWrapGrid Orientation="Horizontal" ItemWidth="280" ItemHeight="118"/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <Border Style="{StaticResource DriveForgeCardStyle}" Margin="0,0,10,10">
                                    <StackPanel Spacing="8"><TextBlock Text="Storage profile" Style="{StaticResource DriveForgeSectionTitleStyle}"/><TextBlock x:Name="MediaClassText" Text="Media: Unknown" Style="{StaticResource DriveForgeCaptionStyle}"/><TextBlock x:Name="BusTypeText" Text="Bus: —" Style="{StaticResource DriveForgeCaptionStyle}"/><TextBlock x:Name="SeekPenaltyText" Text="Seek penalty: Unknown" Style="{StaticResource DriveForgeCaptionStyle}"/><TextBlock x:Name="TrimText" Text="TRIM: Unknown" Style="{StaticResource DriveForgeCaptionStyle}"/></StackPanel>
                                </Border>
                                <Border Style="{StaticResource DriveForgeCardStyle}" Margin="0,0,10,10">
                                    <StackPanel Spacing="8"><TextBlock Text="Performance" Style="{StaticResource DriveForgeSectionTitleStyle}"/><TextBlock x:Name="ApaScanTimeText" Text="APA scan: —" Style="{StaticResource DriveForgeCaptionStyle}"/><TextBlock x:Name="CatalogBuildTimeText" Text="Catalog build: —" Style="{StaticResource DriveForgeCaptionStyle}"/><TextBlock x:Name="AverageReadText" Text="Avg. backing read: —" Style="{StaticResource DriveForgeCaptionStyle}"/></StackPanel>
                                </Border>
                                <Border Style="{StaticResource DriveForgeCardStyle}" Margin="0,0,10,10">
                                    <StackPanel Spacing="8"><TextBlock Text="Safety" Style="{StaticResource DriveForgeSectionTitleStyle}"/><TextBlock Text="✓ Raw source opens read-only" Style="{StaticResource DriveForgeCaptionStyle}"/><TextBlock Text="✓ No public source write API" Style="{StaticResource DriveForgeCaptionStyle}"/><TextBlock Text="✓ Explorer mount rejects writes" Style="{StaticResource DriveForgeCaptionStyle}"/></StackPanel>
                                </Border>
                            </ItemsControl>
                        </Grid>
                    </Grid>
                </ScrollViewer>

                <ScrollViewer x:Name="FilesPage" Grid.Row="1" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Padding="24,20,24,28" MaxWidth="1400" HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <TextBlock Text="Files" Style="{ThemeResource TitleTextBlockStyle}"/>
                        <TextBlock Grid.Row="1" Margin="0,4,0,16" Text="Browse PFS partitions through the same cached read-only DriveSession used by the rest of DriveForge." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        <Border Grid.Row="2" Style="{StaticResource DriveForgeCompactCardStyle}" Margin="0,0,0,12">
                            <Grid ColumnSpacing="8"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <ComboBox x:Name="PfsPartitionPicker" MinWidth="180" PlaceholderText="PFS partition"/>
                                <TextBlock x:Name="FilesPathText" Grid.Column="1" Text="/" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <Button x:Name="FilesUpButton" Grid.Column="2" Content="Up" Style="{StaticResource DriveForgeToolbarButtonStyle}" IsEnabled="False"/>
                                <Button x:Name="ExportButton" Grid.Column="3" Content="Export selected" Style="{StaticResource DriveForgeToolbarButtonStyle}" IsEnabled="False"/>
                            </Grid>
                        </Border>
                        <Border Grid.Row="3" Style="{StaticResource DriveForgeCardStyle}" Padding="0" MinHeight="430">
                            <ListView x:Name="FilesList" SelectionMode="Single" HorizontalContentAlignment="Stretch" Background="Transparent"/>
                        </Border>
                    </Grid>
                </ScrollViewer>

                <ScrollViewer x:Name="MountPage" Grid.Row="1" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Padding="24,20,24,28" MaxWidth="980" HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Text="Mount" Style="{ThemeResource TitleTextBlockStyle}"/>
                        <TextBlock Grid.Row="1" Margin="0,4,0,16" Text="Expose the current source in Explorer through the read-only Dokany helper. The lowest free drive letter from C: to Z: is selected automatically." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        <Border Grid.Row="2" Style="{StaticResource DriveForgeCardStyle}">
                            <StackPanel Spacing="14">
                                <TextBlock x:Name="MountStatusText" Text="Nothing mounted" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock x:Name="MountPointText" Text="Drive letter: automatic" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button x:Name="MountPageMountButton" Content="Mount read-only" IsEnabled="False"/>
                                    <Button x:Name="MountPageExplorerButton" Content="Open in Explorer" IsEnabled="False"/>
                                    <Button x:Name="UnmountButton" Content="Unmount" IsEnabled="False"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>

                <ScrollViewer x:Name="PerformancePage" Grid.Row="1" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Padding="24,20,24,28" MaxWidth="1100" HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Text="Performance" Style="{ThemeResource TitleTextBlockStyle}"/>
                        <TextBlock Grid.Row="1" Margin="0,4,0,16" Text="Live counters from the shared instrumented read-only I/O pipeline." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        <Border Grid.Row="2" Style="{StaticResource DriveForgeCardStyle}">
                            <StackPanel Spacing="10">
                                <TextBlock x:Name="PerfSourceText" Text="No source" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                <TextBlock x:Name="PerfApaText" Text="APA scan: —"/>
                                <TextBlock x:Name="PerfCatalogText" Text="Catalog build: —"/>
                                <TextBlock x:Name="PerfReadsText" Text="Backing reads: —"/>
                                <TextBlock x:Name="PerfBytesText" Text="Backing bytes: —"/>
                                <TextBlock x:Name="PerfAvgText" Text="Average backing read: —" TextWrapping="Wrap"/>
                                <TextBlock x:Name="PerfCacheText" Text="Cache: —" TextWrapping="Wrap"/>
                                <Button x:Name="ResetStatsButton" Content="Reset counters" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>

                <ScrollViewer x:Name="SettingsPage" Grid.Row="1" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Padding="24,20,24,28" MaxWidth="980" HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Text="Settings" Style="{ThemeResource TitleTextBlockStyle}"/>
                        <TextBlock Grid.Row="1" Margin="0,4,0,16" Text="Frontend preferences and diagnostic shortcuts. Storage access remains read-only regardless of theme or frontend." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        <Border Grid.Row="2" Style="{StaticResource DriveForgeCardStyle}" Margin="0,0,0,12">
                            <Grid ColumnSpacing="18"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel><TextBlock Text="Theme" FontWeight="SemiBold"/><TextBlock Text="Use the Windows theme or force light/dark for WinUI." Style="{StaticResource DriveForgeCaptionStyle}" TextWrapping="Wrap"/></StackPanel>
                                <ComboBox x:Name="ThemeSelector" Grid.Column="1" MinWidth="130" SelectedIndex="0"><ComboBoxItem Content="System"/><ComboBoxItem Content="Light"/><ComboBoxItem Content="Dark"/></ComboBox>
                            </Grid>
                        </Border>
                        <Border Grid.Row="3" Style="{StaticResource DriveForgeCardStyle}">
                            <StackPanel Spacing="10">
                                <TextBlock Text="Diagnostics &amp; fallback" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button x:Name="RestartAdminButton" Content="Restart as Administrator"/>
                                    <Button x:Name="OpenLogButton" Content="Open startup log"/>
                                    <Button x:Name="OpenLegacyButton" Content="Open Legacy Win32"/>
                                </StackPanel>
                                <TextBlock Text="Legacy Win32 remains a supported fallback for Emilia 0.5." Style="{StaticResource DriveForgeCaptionStyle}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </NavigationView>

        <Grid Grid.Row="2" Padding="12,0" Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="7" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE946;" FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock x:Name="StatusText" Text="Ready — scanning for PS2 HDDs" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="11" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <TextBlock Grid.Column="1" Text="WinUI 3 · Windows App SDK 2.4.0" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="11" TextTrimming="CharacterEllipsis"/>
        </Grid>
    </Grid>
</Window>